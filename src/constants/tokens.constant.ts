/**
 * DI tokens and metadata keys for the logger system.
 *
 * @module @stackra/ts-logger/constants
 */

/**
 * Metadata key used by the `@Reporter` decorator to mark a class
 * as a log reporter for auto-discovery by the `ReporterLoader`.
 */
export const REPORTER_METADATA = "REPORTER_METADATA";
